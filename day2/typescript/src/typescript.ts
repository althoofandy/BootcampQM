function greeter(name: string) {
  return `Hello, ${name}!`;
}

console.log(greeter("John"));

let num: number;
num = 10;

interface Person {
  name: string;
  age: number;
}

let person: Person = {
  name: "John",
  age: 30,
};
